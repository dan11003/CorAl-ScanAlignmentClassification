# Parameters for experiments

# Define a list per each parameter here (add as many as necessary)
rangeError = [0.1, 0.3, 0.5, 0.7, 0.9]
methods = ['P2P','P2D','P2L']

# Include lists here
parameters = [rangeError, methods]
